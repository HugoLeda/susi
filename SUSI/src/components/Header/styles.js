import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  containerHead: {
    backgroundColor: 'rgba(153, 185, 255, 0.75)',
    paddingTop: 25,
    alignSelf: 'stretch',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
